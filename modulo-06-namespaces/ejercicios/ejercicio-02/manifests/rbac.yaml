apiVersion: v1
kind: ServiceAccount
metadata:
  name: # TODO: nombre del ServiceAccount
  namespace: # TODO: namespace del ServiceAccount
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: dev-reader-role
  namespace: # TODO: namespace donde aplica el Role
rules:
  - apiGroups: [""]
    resources:
      - pods
      - services
      - configmaps
    verbs:
      - get
      - list
      - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: dev-reader-binding
  namespace: # TODO: namespace del binding correcto
subjects:
  - kind: ServiceAccount
    name: # TODO: nombre del ServiceAccount
    namespace: # TODO: namespace del ServiceAccount
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: dev-reader-role
---
# FIX: este bloque otorga acceso en prod y contradice el objetivo del ejercicio.
# FIX: corrígelo para que el ServiceAccount solo tenga permisos en dev.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: prod-reader-role
  namespace: prod
rules:
  - apiGroups: [""]
    resources:
      - pods
    verbs:
      - get
      - list
      - watch
---
# FIX: este RoleBinding es incorrecto para el escenario final.
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: prod-reader-binding
  namespace: prod
subjects:
  - kind: ServiceAccount
    name: dev-reader
    namespace: dev
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: prod-reader-role
