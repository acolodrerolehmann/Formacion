kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: cilium-cluster
nodes:
  - role: control-plane
  - role: worker
  - role: worker
networking:
  disableDefaultCNI: true      # Don't use kindnet — Cilium replaces it
  kubeProxyMode: none          # Cilium replaces kube-proxy too