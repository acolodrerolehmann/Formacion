apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-clusterip
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-clusterip
  template:
    metadata:
      labels:
        app: hello-clusterip
    spec:
      containers:
      - name: web
        # TODO: especificar la imagen (ej: nginx:alpine)
        image: TODO_IMAGE
        ports:
        - containerPort: 80
