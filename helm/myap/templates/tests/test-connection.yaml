apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myap.fullname" . }}-test-connection"
  labels:
    {{- include "myap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
