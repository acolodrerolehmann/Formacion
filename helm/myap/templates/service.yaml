apiVersion: v1
kind: Service
metadata:
  name: {{ include "myap.fullname" . }}
  labels:
    {{- include "myap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myap.selectorLabels" . | nindent 4 }}
