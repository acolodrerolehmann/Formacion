# FIX: La versión de la API es incorrecta
apiVersion: v1beta1  # ← corregir
kind: Pod
metadata:
  # FIX: Falta el nombre del pod
  name:  # ← completar
  labels:
    app: diagnostico
spec:
  containers:
    - # FIX: Falta el nombre del contenedor
      name:  # ← completar
      # FIX: El formato de la imagen es inválido
      image: nginx::latest  # ← corregir
      ports:
        - containerPort: 80
