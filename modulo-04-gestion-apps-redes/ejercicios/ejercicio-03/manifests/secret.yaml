apiVersion: v1
kind: Secret
metadata:
  name: order-api-credentials
type: Opaque
# TODO: Añade la sección 'stringData' con las credenciales:
# - DB_USER (usuario de la base de datos)
# - DB_PASSWORD (contraseña de la base de datos)
#
# Nota: 'stringData' acepta texto plano y Kubernetes lo codifica automáticamente en base64.
# Alternativa: puedes usar 'data' con los valores ya codificados en base64.
