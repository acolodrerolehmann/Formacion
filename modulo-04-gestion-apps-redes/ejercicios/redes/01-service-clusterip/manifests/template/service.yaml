apiVersion: v1
kind: Service
metadata:
  name: hello-clusterip
spec:
  type: ClusterIP  # TODO: para cambiar a NodePort editar este campo
  selector:
    app: hello-clusterip
  ports:
  - port: 80
    targetPort: 80
