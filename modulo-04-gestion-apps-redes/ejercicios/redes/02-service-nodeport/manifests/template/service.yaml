apiVersion: v1
kind: Service
metadata:
  name: hello-nodeport
spec:
  type: NodePort  # TODO: definir nodePort si se desea puerto concreto
  selector:
    app: hello-nodeport
  ports:
  - port: 80
    targetPort: 80
    # nodePort: 30080
